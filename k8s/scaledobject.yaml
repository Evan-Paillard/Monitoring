apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: monitoring-app
  namespace: monitoring
spec:
  scaleTargetRef:
    name: monitoring-app
  minReplicaCount: 2
  maxReplicaCount: 5
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "60"
