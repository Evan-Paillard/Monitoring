apiVersion: v1
kind: Service
metadata:
  name: monitoring-app
  namespace: monitoring
  labels:
    app: monitoring-app
spec:
  selector:
    app: monitoring-app
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
